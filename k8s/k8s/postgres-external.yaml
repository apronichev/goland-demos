apiVersion: v1
kind: Service
metadata:
  name: postgres-external
  namespace: k8s-demo
  labels:
    app: k8s-demo
spec:
  type: ExternalName
  externalName: host.docker.internal
  # Port 54338 must be specified in connection string
  # Note: ExternalName services do not use ports configuration

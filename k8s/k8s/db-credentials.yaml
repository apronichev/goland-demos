apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: k8s-demo
  labels:
    app: k8s-demo
type: Opaque
stringData:
  # PostgreSQL database credentials
  db.user: "guest"
  db.password: "guest"

  # Redis password (optional - currently not required)
  redis.password: ""
